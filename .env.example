HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Mercado Pago (Checkout Pro)
# Backend credentials (server-side only)
MERCADOPAGO_ACCESS_TOKEN=tobemodified

# Optional: used by the future frontend
MERCADOPAGO_PUBLIC_KEY=

# Public URLs used to build redirect URLs + webhook notification_url
PUBLIC_BACKEND_URL=http://localhost:1337
FRONTEND_URL=http://localhost:3000

# Optional: webhook protection. If set, append `?token=...` to the webhook URL you configure in Mercado Pago.
MERCADOPAGO_WEBHOOK_TOKEN=tobemodified

# Optional: if set, overrides the auto-built notification_url.
MERCADOPAGO_NOTIFICATION_URL=

# Currency for MP preference items (Argentina usually ARS)
MERCADOPAGO_CURRENCY=ARS
